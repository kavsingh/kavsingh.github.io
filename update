#!/bin/bash

reloadFlag=$([ "$RELOAD" == "true" ] && echo "--reload" || echo "")
cmd="deno $reloadFlag --allow-env --allow-read --allow-write=. --allow-run scripts/bin/update.ts "$@""

echo $cmd
$cmd
